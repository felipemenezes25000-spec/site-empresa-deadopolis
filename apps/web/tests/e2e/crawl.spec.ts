import { expect, test } from "@playwright/test";
const routes=["/","/servicos","/noticias","/secretarias","/transparencia","/diario-oficial","/buscar?q=IPTU","/acesso-a-informacao","/ouvidoria","/dados-abertos","/agenda","/locais","/contatos","/licitacoes","/legislacao","/privacidade","/acessibilidade","/demo/modernization","/admin/login"];
test("rotas públicas críticas não retornam 404/500",async({page,request})=>{for(const route of routes){const response=await request.get(route);expect(response.status(),route).toBeLessThan(400);await page.goto(route);await expect(page.locator("body")).not.toContainText("Application error");const brokenImages=await page.locator("img").evaluateAll(images=>images.filter(image=>!(image as HTMLImageElement).complete||(image as HTMLImageElement).naturalWidth===0).map(image=>(image as HTMLImageElement).src));expect(brokenImages,`imagens quebradas em ${route}`).toEqual([])}});
