import type { Metadata } from "next";import { PageIntro,PublicShell } from "@/components/portal/public-shell";import { getDepartments,getResources } from "@/lib/portal-api";export const metadata:Metadata={title:"Contatos"};export default async function Page(){const[departments,contacts]=await Promise.all([getDepartments(),getResources("CONTACT")]);return <PublicShell><PageIntro eyebrow="Quem procuro?" title="Contatos da Prefeitura" description="Diretório pesquisável de secretarias, setores e canais de atendimento."/><section className="content-section"><div className="page-shell"><div className="card-grid">{departments.map(item=><article className="info-card" key={item.slug}><span className="kicker">{item.acronym}</span><h2>{item.name}</h2><p>{item.phone||"Telefone em atualização"}<br/>{item.email||"E-mail em atualização"}</p><small>{item.address||item.openingHours}</small></article>)}{contacts.map(item=><article className="info-card" key={item.id}><span className="kicker">Contato</span><h2>{item.title}</h2><p>{item.summary}</p></article>)}</div><div id="emergencia" className="warning-box" style={{marginTop:28}}><strong>Emergências:</strong> canais de urgência devem ser validados pela Prefeitura antes do go-live; a demonstração não inventa telefones oficiais.</div></div></section></PublicShell>}
