import type { Metadata } from "next";import { ManagedResourcePage } from "@/components/portal/managed-resource-page";import { getResource } from "@/lib/portal-api";export const metadata:Metadata={title:"Privacidade e LGPD"};export default async function Page(){return <ManagedResourcePage resource={await getResource("PAGE","privacidade")} eyebrow="LGPD" fallbackTitle="Privacidade e proteção de dados" fallbackDescription="A política jurídica definitiva e o contato do encarregado devem ser validados pela Prefeitura antes da produção."/>}
