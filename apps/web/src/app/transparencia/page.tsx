import type { Metadata } from "next";
import { PageIntro, PublicShell } from "@/components/portal/public-shell";
import { getTransparency } from "@/lib/portal-api";
export const metadata:Metadata={title:"Transparência"};
export default async function TransparencyPage(){const links=await getTransparency();const fixed=[{title:"Diário Oficial",description:"Atos e publicações",url:"/diario-oficial"},{title:"Dados Abertos",description:"Catálogo de datasets",url:"/dados-abertos"},{title:"Acesso à Informação",description:"e-SIC e orientações",url:"/acesso-a-informacao"},{title:"Ouvidoria",description:"Participação social",url:"/ouvidoria"},{title:"Licitações",description:"Compras públicas",url:"/licitacoes"}];return <PublicShell><PageIntro eyebrow="Governo aberto" title="Transparência e participação" description="Um ponto de entrada claro para receitas, despesas, orçamento, licitações, contratos e controle social."/><section className="content-section"><div className="page-shell card-grid">{[...links,...fixed].map((item,index)=><a className="info-card" key={`${item.title}-${index}`} href={item.url} target={item.url.startsWith("http")?"_blank":undefined} rel={item.url.startsWith("http")?"noreferrer":undefined}><span className="kicker">{item.url.startsWith("http")?"Sistema externo":"Portal municipal"}</span><h2>{item.title}</h2><p>{item.description}</p>{item.url.startsWith("http")&&<small>Você será direcionado para um sistema externo.</small>}</a>)}</div></section></PublicShell>}
