import type { MetadataRoute } from "next";const paths=["/","/servicos","/noticias","/secretarias","/transparencia","/diario-oficial","/dados-abertos","/acesso-a-informacao","/ouvidoria","/licitacoes","/legislacao","/agenda","/locais","/contatos","/acessibilidade","/privacidade"];export default function sitemap():MetadataRoute.Sitemap{const base=process.env.PUBLIC_PORTAL_URL??"http://localhost:3000";return paths.map(path=>({url:`${base}${path}`,changeFrequency:path==="/"?"daily":"weekly",priority:path==="/"?1:.7}))}
