import type { Metadata } from "next";import { ManagedResourcePage } from "@/components/portal/managed-resource-page";import { getResource } from "@/lib/portal-api";export const metadata:Metadata={title:"Acesso à Informação"};export default async function Page(){return <ManagedResourcePage resource={await getResource("PAGE","acesso-a-informacao")} eyebrow="e-SIC" fallbackTitle="Acesso à Informação" fallbackDescription="Orientações, prazos e acesso ao canal eletrônico de pedidos de informação."/>}
