import type { Metadata } from "next";import { EmptyPanel,PageIntro,PublicShell } from "@/components/portal/public-shell";import { getResources } from "@/lib/portal-api";export const metadata:Metadata={title:"Legislação"};export default async function Page(){const items=await getResources("LEGISLATION");return <PublicShell><PageIntro eyebrow="Normas municipais" title="Legislação" description="Pesquisa e acesso ao acervo legislativo com origem e sistema responsável claramente identificados."/><section className="content-section"><div className="page-shell">{items.length===0?<EmptyPanel title="Acervo em integração" description="A integração ou migração do sistema legislativo será publicada sem apagar a origem histórica."/>:<div className="card-grid">{items.map(item=><article className="info-card" key={item.id}><span className="kicker">Legislação</span><h2>{item.title}</h2><p>{item.summary}</p></article>)}</div>}</div></section></PublicShell>}
