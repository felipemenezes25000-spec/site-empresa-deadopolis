import { EmptyPanel, PageIntro, PublicShell } from "./public-shell";
import type { PortalResource } from "@/lib/portal-api";
export function ManagedResourcePage({resource,eyebrow,fallbackTitle,fallbackDescription}:{resource:PortalResource|null;eyebrow:string;fallbackTitle:string;fallbackDescription:string}){if(!resource)return <PublicShell><PageIntro eyebrow={eyebrow} title={fallbackTitle} description={fallbackDescription}/><section className="content-section"><div className="page-shell"><EmptyPanel title="Conteúdo em atualização" description="Esta área é administrável pelo CMS municipal e ainda não possui publicação ativa."/></div></section></PublicShell>;return <PublicShell><PageIntro eyebrow={eyebrow} title={resource.title} description={resource.summary}/><section className="content-section"><div className="page-shell detail-grid"><article className="prose-card"><h2>Informações</h2><ResourcePayload payload={resource.payload}/></article><aside className="side-card"><h2>Governança do conteúdo</h2><p>Esta página é publicada pelo CMS, possui versionamento e trilha de auditoria.</p><small>Versão {resource.version}{resource.publishedAt?` · publicada em ${new Intl.DateTimeFormat("pt-BR").format(new Date(resource.publishedAt))}`:""}</small></aside></div></section></PublicShell>}
function ResourcePayload({payload}:{payload:unknown}){if(!payload||typeof payload!=="object")return <p>Informação estruturada indisponível.</p>;const entries=Object.entries(payload as Record<string,unknown>).filter(([key])=>key!=="classification");return <dl className="definition-list">{entries.map(([key,value])=><div style={{display:"contents"}} key={key}><dt>{humanize(key)}</dt><dd>{Array.isArray(value)?value.join(", "):typeof value==="object"?JSON.stringify(value):String(value)}</dd></div>)}</dl>}
function humanize(value:string){return value.replace(/([A-Z])/g," $1").replace(/^./,c=>c.toUpperCase())}
