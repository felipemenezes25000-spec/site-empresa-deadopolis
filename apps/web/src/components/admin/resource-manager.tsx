"use client";
import { useCallback,useEffect,useState,type FormEvent } from "react";type Resource={id:string;kind:string;slug:string;title:string;summary:string;status:string;displayOrder:number;version:number};const kinds=["PAGE","BANNER","EVENT","LEGISLATION","DATASET","LOCATION","CONTACT","ALERT","MENU","HOME_BLOCK","PROCUREMENT_LINK","ESIC_LINK","OUVIDORIA_LINK"];
export function ResourceManager(){const[items,setItems]=useState<Resource[]>([]);const[kind,setKind]=useState("PAGE");const[message,setMessage]=useState("");const load=useCallback(async()=>{const r=await fetch(`/api/v1/admin/resources?kind=${kind}`);if(r.ok)setItems(await r.json() as Resource[])},[kind]);useEffect(()=>{void load()},[load]);async function create(event:FormEvent<HTMLFormElement>){event.preventDefault();const form=new FormData(event.currentTarget);const payload={kind,slug:form.get("slug"),title:form.get("title"),summary:form.get("summary"),payloadJson:String(form.get("payloadJson")||"{}"),displayOrder:Number(form.get("displayOrder")||0),startsAt:null,endsAt:null};const r=await fetch("/api/v1/admin/resources",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(payload)});setMessage(r.ok?"Conteúdo criado como rascunho.":await errorText(r));if(r.ok){event.currentTarget.reset();await load()}}async function transition(id:string,action:string){const r=await fetch(`/api/v1/admin/resources/${id}/${action}`,{method:"POST"});setMessage(r.ok?`Ação ${action} concluída.`:await errorText(r));await load()}return <div className="editor-grid"><section className="admin-panel"><div className="resource-toolbar"><label>Tipo <select value={kind} onChange={e=>setKind(e.target.value)}>{kinds.map(value=><option key={value}>{value}</option>)}</select></label></div>{items.length===0?<div className="empty-state"><h3>Nenhum conteúdo deste tipo</h3><p>Crie um item no formulário ao lado.</p></div>:<div className="compact-list">{items.map(item=><div className="compact-item" key={item.id}><div><strong>{item.title}</strong><small style={{display:"block"}}>{item.slug} · v{item.version}</small></div><div className="button-row"><span className="status-pill">{item.status}</span>{item.status!=="PUBLISHED"&&item.status!=="ARCHIVED"&&<button type="button" className="action-button" onClick={()=>transition(item.id,"publish")}>Publicar</button>}{item.status!=="ARCHIVED"&&<button type="button" className="action-button secondary" onClick={()=>transition(item.id,"archive")}>Arquivar</button>}{item.status==="ARCHIVED"&&<button type="button" className="action-button secondary" onClick={()=>transition(item.id,"restore")}>Restaurar</button>}</div></div>)}</div>}{message&&<div className="form-message">{message}</div>}</section><form className="admin-panel editor-fields" onSubmit={create}><h2>Novo conteúdo</h2><label className="field">Título<input name="title" required maxLength={220}/></label><label className="field">Slug<input name="slug" required pattern="[a-z0-9-]+"/></label><label className="field">Resumo<textarea name="summary" rows={3} maxLength={500}/></label><label className="field">Ordem<input name="displayOrder" type="number" defaultValue={0}/></label><label className="field">Detalhes estruturados (JSON)<textarea name="payloadJson" rows={8} defaultValue="{}"/><small>Campo avançado. Formulários especializados podem ser adicionados sem alterar o modelo de persistência.</small></label><button className="action-button">Salvar rascunho</button></form></div>}
async function errorText(r:Response){const b=await r.json().catch(()=>null) as {title?:string;detail?:string;errors?:Record<string,string[]>}|null;const validation=Object.values(b?.errors??{}).flat().join(" ");return b?.detail??b?.title??(validation||`Erro ${r.status}`)}
