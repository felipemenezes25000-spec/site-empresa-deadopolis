"use client";
import Link from "next/link";import { useEffect,useState } from "react";type Article={id:string;title:string;slug:string;status:string;version:number;updatedAt:string;scheduledFor:string|null};export function NewsList(){const[items,setItems]=useState<Article[]>([]);const[error,setError]=useState("");useEffect(()=>{fetch("/api/v1/admin/news").then(async r=>{if(!r.ok)throw new Error(`Erro ${r.status}`);setItems(await r.json() as Article[])}).catch(e=>setError(e instanceof Error?e.message:"Falha"))},[]);return <section className="admin-panel"><div className="admin-heading"><div><h2>Publicações editoriais</h2><p>Rascunhos, revisão, agenda e publicações recentes.</p></div><Link className="action-button" href="/admin/noticias/nova">+ Nova notícia</Link></div>{error&&<div className="form-message error">{error}</div>}{items.length===0?<div className="empty-state"><h3>Nenhuma notícia</h3><p>Crie a primeira publicação pelo editor.</p></div>:<table className="admin-table"><thead><tr><th>Título</th><th>Status</th><th>Versão</th><th>Atualização</th></tr></thead><tbody>{items.map(item=><tr key={item.id}><td><strong>{item.title}</strong><br/><small>/{item.slug}</small></td><td><span className="status-pill">{item.status}</span></td><td>{item.version}</td><td>{new Intl.DateTimeFormat("pt-BR",{dateStyle:"short",timeStyle:"short"}).format(new Date(item.updatedAt))}</td></tr>)}</tbody></table>}</section>}
