// Canonical requirement pointer: the executable POC test lives in apps/web/tests/e2e/poc.spec.ts.
// It is executed by `npm --prefix apps/web run test:e2e` and by the GitHub Actions E2E job.
// This root path is intentionally retained because the project specification names tests/e2e/poc.spec.ts as evidence.
